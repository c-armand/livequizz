const gameConfig = {
  questionsByGame: 15,     // questions by game
  answerDelay: 18000,      // time to find answer
  nextQuestionDelay: 5000, // time before next question
  nextGameDelay: 30000     // time before next game
}

module.exports = gameConfig